# Project Sekai 2DMV Database 重构实施方案

## 📋 项目概述

### 目标
将现有的扁平化视频数据结构重构为以歌曲为中心的关系型数据库，整合 YouTube 视频信息、Sekai Viewer 游戏数据和手动维护的元数据（如别称）。

### 核心价值
- ✅ **消除数据冗余**：歌曲信息只存储一次
- ✅ **提升可维护性**：修改歌曲信息只需改一处
- ✅ **支持多类型视频**：2DMV、3DMV、翻唱、演唱会等
- ✅ **预整合游戏数据**：无需运行时匹配，提升性能
- ✅ **支持别称系统**：统一管理歌曲别称

### 预期成果
- 新的数据文件：`database_v2.json`
- 数据构建脚本：`build_database.py`
- 更新的前端代码：`index.html`
- 别称维护文件：`manual_data/aliases.json`

---

## 🗂️ 数据结构设计

### 顶层结构

```json
{
  "metadata": { ... },
  "songs": [ ... ]
}
```

### metadata 字段详解

```json
{
  "metadata": {
    "version": "2.0.0",
    "generatedAt": "2026-01-22T15:41:34Z",
    "sources": [
      "YouTube API (playlist_videos_20260122_154134.json)",
      "Sekai Viewer musics.json",
      "Sekai Viewer musicTag.json",
      "Manual aliases.json"
    ],
    "stats": {
      "totalSongs": 450,
      "totalVideos": 240,
      "videoTypeBreakdown": {
        "official_2dmv": 200,
        "official_3dmv": 0,
        "vs_version": 40,
        "cover": 0,
        "live": 0
      },
      "unitBreakdown": {
        "Leo/need": 85,
        "MORE MORE JUMP!": 78,
        "Vivid BAD SQUAD": 92,
        "ワンダショ": 81,
        "25時、ナイトコードで。": 88,
        "Virtual Singer": 26
      }
    }
  }
}
```

### songs 数组字段详解

```json
{
  "songs": [
    {
      // ===== 唯一标识 =====
      "id": "song_幸福刑",
      "sekaiMusicId": 450,

      // ===== 标题信息 =====
      "title": "幸福刑",
      "titleJp": "幸福刑",
      "titleRomaji": "Koufukukei",
      "titleEn": null,

      // ===== 别称系统 ⭐ =====
      "aliases": [
        "koufukukei",
        "幸福罪",
        "幸刑"
      ],

      // ===== 创作者信息 =====
      "creators": {
        "composer": "LonePi",
        "lyricist": "LonePi",
        "arranger": "LonePi",
        "illustrator": "火種",
        "videoEditor": "Riesz",
        "lyricDesigner": "Heetami"
      },

      // ===== 分类标签 =====
      "classification": {
        "units": ["25時、ナイトコードで。"],
        "virtualSingers": ["MEIKO"],
        "category": "original",
        "mvType": "mv_2d",
        "tags": [
          "游戏2D MV",
          "官方MV",
          "25時、ナイトコードで。",
          "MEIKO",
          "2026年"
        ]
      },

      // ===== 时间信息 =====
      "dates": {
        "sekaiReleaseDate": "2026-01-20",
        "youtubeUploadDate": "2026-01-20T01:38:14Z",
        "originalReleaseDate": null
      },

      // ===== 游戏数据（可选）=====
      "gameData": {
        "difficulty": {
          "easy": null,
          "normal": null,
          "hard": null,
          "expert": null,
          "master": null
        },
        "duration": 150,
        "bpm": 175,
        "publishedAt": 1737341894000
      },

      // ===== 视频列表 ⭐ =====
      "videos": [
        {
          "type": "official_2dmv",
          "variant": "unit_version",
          "videoId": "pWc8oCbtGKc",
          "url": "https://www.youtube.com/watch?v=pWc8oCbtGKc",
          "title": "幸福刑 / 25時、ナイトコードで。 × MEIKO",
          "description": "『幸福刑』\n25時、ナイトコードで。 × MEIKO\n...",
          "channelTitle": "プロジェクトセカイ カラフルステージ! feat. 初音ミク",
          "channelId": "UCdMGYXL38w6htx6Yf9YJa-w",
          "uploadDate": "2026-01-20T01:38:14Z",
          "thumbnails": {
            "default": "https://i.ytimg.com/vi/pWc8oCbtGKc/default.jpg",
            "medium": "https://i.ytimg.com/vi/pWc8oCbtGKc/mqdefault.jpg",
            "high": "https://i.ytimg.com/vi/pWc8oCbtGKc/hqdefault.jpg",
            "standard": "https://i.ytimg.com/vi/pWc8oCbtGKc/sddefault.jpg",
            "maxres": "https://i.ytimg.com/vi/pWc8oCbtGKc/maxresdefault.jpg"
          },
          "playlistPosition": 0
        }
      ]
    }
  ]
}
```

### 视频类型定义

| type | 说明 | variant 可选值 |
|------|------|---------------|
| `official_2dmv` | 官方2D动画MV | `unit_version`, `vs_version` |
| `official_3dmv` | 官方3D游戏MV | `unit_version`, `vs_version` |
| `game_size` | 游戏尺寸MV | `unit_version`, `vs_version` |
| `cover` | 翻唱视频 | `fan_made`, `official_cover` |
| `live` | 演唱会Live | `concert_2023`, `concert_2024` |
| `dance_practice` | 舞蹈练习 | - |
| `behind_the_scenes` | 幕后花絮 | - |

---

## 🛠️ 实施步骤

### 阶段一：数据准备（第1-2天）

#### 1.1 创建目录结构

```bash
Project Sekai 2DMV Database/
├── manual_data/              # 新建：手动维护数据
│   ├── aliases.json         # 歌曲别称
│   ├── creators.json        # 创作者信息补充
│   └── corrections.json     # 数据修正
├── scripts/                  # 新建：构建脚本
│   ├── build_database.py    # 主构建脚本
│   ├── validate_data.py     # 数据验证脚本
│   └── utils.py             # 工具函数
├── output/                   # 新建：输出目录
│   └── database_v2.json     # 生成的新数据库
├── backup/                   # 新建：备份目录
│   └── index_v1.html        # 旧版本备份
└── (现有文件...)
```

#### 1.2 创建 aliases.json

**文件路径**: `manual_data/aliases.json`

```json
{
  "幸福刑": {
    "aliases": ["koufukukei", "幸福罪"],
    "notes": "LonePi作曲，ニーゴ歌曲"
  },
  "星空のメロディー": {
    "aliases": ["hoshizora no melody", "星空旋律"],
    "notes": "ワンダショ歌曲"
  },
  "モザイクロール": {
    "aliases": ["mosaic roll", "马赛克卷"],
    "notes": "DECO*27原曲"
  },
  "エイリアンエイリアン": {
    "aliases": ["AA", "alien alien", "外星人外星人"],
    "notes": "ナユタン星人原曲"
  },
  "ロキ": {
    "aliases": ["loki", "洛基", "roki"],
    "notes": "みきとP原曲"
  }
}
```

#### 1.3 创建 corrections.json（数据修正）

```json
{
  "titleCorrections": {
    "星空のメロディー": "星空のメロディー",
    "モザイクロール (Reloaded)": "モザイクロール"
  },
  "creatorCorrections": {
    "幸福刑": {
      "illustrator": "火種",
      "videoEditor": "Riesz"
    }
  }
}
```

---

### 阶段二：构建脚本开发（第3-4天）

#### 2.1 安装依赖

```bash
pip install python-dateutil
```

#### 2.2 创建主构建脚本

**文件路径**: `scripts/build_database.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Project Sekai 2DMV Database Builder
将多个数据源整合成统一的数据库
"""

import json
import os
from datetime import datetime
from pathlib import Path
from typing import Dict, List, Any, Optional
import re

class DatabaseBuilder:
    def __init__(self, base_path: str):
        self.base_path = Path(base_path)
        self.youtube_data = None
        self.sekai_musics = []
        self.sekai_music_tags = []
        self.sekai_units = []
        self.aliases = {}
        self.corrections = {}

        # 输出统计
        self.stats = {
            'total_songs': 0,
            'total_videos': 0,
            'matched_sekai': 0,
            'unmatched_videos': [],
            'video_type_breakdown': {},
            'unit_breakdown': {}
        }

    def load_data(self):
        """加载所有数据源"""
        print("📂 正在加载数据源...")

        # 1. 加载 YouTube 数据
        youtube_path = self.base_path / "fetch_youtube_playlist" / "playlist_videos_20260122_154134.json"
        with open(youtube_path, 'r', encoding='utf-8') as f:
            self.youtube_data = json.load(f)
        print(f"✅ YouTube 数据加载完成: {len(self.youtube_data['videos'])} 个视频")

        # 2. 加载 Sekai Viewer 数据
        sekai_path = self.base_path / "sekai viewer_json"

        with open(sekai_path / "musics.json", 'r', encoding='utf-8') as f:
            self.sekai_musics = json.load(f)
        print(f"✅ Sekai 音乐数据: {len(self.sekai_musics)} 首歌曲")

        with open(sekai_path / "musicTag.json", 'r', encoding='utf-8') as f:
            self.sekai_music_tags = json.load(f)
        print(f"✅ Sekai 标签数据: {len(self.sekai_music_tags)} 条记录")

        with open(sekai_path / "unitProfiles.json", 'r', encoding='utf-8') as f:
            self.sekai_units = json.load(f)
        print(f"✅ Sekai 组合数据: {len(self.sekai_units)} 个组合")

        # 3. 加载手动维护数据
        manual_path = self.base_path / "manual_data"

        if (manual_path / "aliases.json").exists():
            with open(manual_path / "aliases.json", 'r', encoding='utf-8') as f:
                self.aliases = json.load(f)
            print(f"✅ 别称数据: {len(self.aliases)} 首歌曲")

        if (manual_path / "corrections.json").exists():
            with open(manual_path / "corrections.json", 'r', encoding='utf-8') as f:
                self.corrections = json.load(f)
            print(f"✅ 修正数据加载完成")

    def extract_song_title(self, video_title: str) -> str:
        """从视频标题中提取歌曲名"""
        # 示例: "幸福刑 / 25時、ナイトコードで。 × MEIKO" → "幸福刑"
        # 示例: "モザイクロール (Reloaded) / Leo/need × KAITO" → "モザイクロール"

        # 去除 (Reloaded) 等后缀
        title = re.sub(r'\s*\(.*?\)\s*', '', video_title)

        # 提取 / 之前的部分
        if ' / ' in title:
            song_title = title.split(' / ')[0].strip()
        else:
            song_title = title.strip()

        # 应用修正
        if 'titleCorrections' in self.corrections:
            song_title = self.corrections['titleCorrections'].get(song_title, song_title)

        return song_title

    def match_sekai_music(self, song_title: str) -> Optional[Dict]:
        """匹配 Sekai 音乐数据"""
        song_title_lower = song_title.lower()

        for music in self.sekai_musics:
            music_title_lower = music['title'].lower()

            # 完全匹配
            if song_title_lower == music_title_lower:
                return music

            # 包含匹配
            if song_title_lower in music_title_lower or music_title_lower in song_title_lower:
                return music

        return None

    def get_music_units(self, music_id: int) -> List[str]:
        """获取歌曲所属组合"""
        unit_mapping = {
            'light_music_club': 'Leo/need',
            'idol': 'MORE MORE JUMP!',
            'street': 'Vivid BAD SQUAD',
            'theme_park': 'ワンダショ',
            'school_refusal': '25時、ナイトコードで。',
            'vocaloid': 'Virtual Singer'
        }

        units = []
        for tag_entry in self.sekai_music_tags:
            if tag_entry['musicId'] == music_id:
                tag_type = tag_entry['musicTag']
                if tag_type in unit_mapping:
                    unit_name = unit_mapping[tag_type]
                    if unit_name not in units:
                        units.append(unit_name)

        return units

    def extract_virtual_singers(self, video_title: str) -> List[str]:
        """从视频标题提取虚拟歌手"""
        singers = []
        singer_patterns = {
            '初音ミク': ['初音ミク', 'Miku'],
            '鏡音リン': ['鏡音リン', 'Rin'],
            '鏡音レン': ['鏡音レン', 'Len'],
            '巡音ルカ': ['巡音ルカ', 'Luka'],
            'MEIKO': ['MEIKO'],
            'KAITO': ['KAITO']
        }

        for singer, patterns in singer_patterns.items():
            for pattern in patterns:
                if pattern in video_title:
                    singers.append(singer)
                    break

        return singers

    def determine_video_type(self, video: Dict, song_title: str) -> str:
        """判断视频类型"""
        title = video['title'].lower()
        desc = video['description'].lower()

        # 2DMV 检测
        if '2dmv' in title or 'アニメーション' in desc or '動画：' in video['description']:
            return 'official_2dmv'

        # 3DMV 检测
        if '3dmv' in title or 'game mv' in title:
            return 'official_3dmv'

        # 默认返回 official_2dmv（因为当前数据都是官方MV）
        return 'official_2dmv'

    def determine_variant(self, video: Dict) -> str:
        """判断视频变体"""
        title = video['title']

        # 检查是否包含组合名
        units = ['Leo/need', 'MORE MORE JUMP', 'Vivid BAD SQUAD',
                 'ワンダーランズ', '25時、ナイトコード']

        for unit in units:
            if unit in title:
                return 'unit_version'

        # 检查是否是纯虚拟歌手版本
        if 'バーチャル・シンガー' in title or 'Virtual Singer' in title:
            return 'vs_version'

        return 'unit_version'

    def build_song_entry(self, song_title: str, videos: List[Dict]) -> Dict:
        """构建单个歌曲条目"""
        # 匹配 Sekai 数据
        sekai_music = self.match_sekai_music(song_title)

        if sekai_music:
            self.stats['matched_sekai'] += 1
            sekai_music_id = sekai_music['id']
            units = self.get_music_units(sekai_music_id)
            mv_type = 'mv_2d' if 'mv_2d' in sekai_music.get('categories', []) else 'mv'
        else:
            sekai_music_id = None
            units = []
            mv_type = None

        # 提取虚拟歌手
        virtual_singers = []
        for video in videos:
            singers = self.extract_virtual_singers(video['title'])
            for singer in singers:
                if singer not in virtual_singers:
                    virtual_singers.append(singer)

        # 获取别称
        aliases = []
        if song_title in self.aliases:
            aliases = self.aliases[song_title].get('aliases', [])

        # 生成标签
        tags = []
        if mv_type == 'mv_2d':
            tags.append('游戏2D MV')
        elif mv_type == 'mv':
            tags.append('游戏3D MV')
        tags.append('官方MV')
        tags.extend(units)
        tags.extend(virtual_singers)

        # 添加年份标签
        if videos:
            year = videos[0]['publishedAt'][:4]
            tags.append(f'{year}年')

        # 构建视频列表
        video_entries = []
        for video in videos:
            video_entry = {
                'type': self.determine_video_type(video, song_title),
                'variant': self.determine_variant(video),
                'videoId': video['videoId'],
                'url': video['url'],
                'title': video['title'],
                'description': video['description'],
                'channelTitle': video['channelTitle'],
                'channelId': video['channelId'],
                'uploadDate': video['publishedAt'],
                'thumbnails': video['thumbnails'],
                'playlistPosition': video['position']
            }
            video_entries.append(video_entry)

        # 构建歌曲条目
        song_entry = {
            'id': f'song_{song_title}',
            'sekaiMusicId': sekai_music_id,
            'title': song_title,
            'titleJp': song_title,
            'titleRomaji': None,
            'titleEn': None,
            'aliases': aliases,
            'creators': self._extract_creators(videos[0]) if videos else {},
            'classification': {
                'units': units,
                'virtualSingers': virtual_singers,
                'category': 'original',
                'mvType': mv_type,
                'tags': tags
            },
            'dates': {
                'sekaiReleaseDate': videos[0]['publishedAt'][:10] if videos else None,
                'youtubeUploadDate': videos[0]['publishedAt'] if videos else None,
                'originalReleaseDate': None
            },
            'gameData': self._extract_game_data(sekai_music) if sekai_music else {},
            'videos': video_entries
        }

        return song_entry

    def _extract_creators(self, video: Dict) -> Dict:
        """从视频描述提取创作者信息"""
        desc = video['description']
        creators = {}

        # 提取作词作曲
        patterns = {
            'composer': r'作詞・作曲[：:]\s*([^\n\s]+)',
            'lyricist': r'作詞[：:]\s*([^\n\s]+)',
            'illustrator': r'イラスト[：:]\s*([^\n\s]+)',
            'videoEditor': r'動画[：:]\s*([^\n\s]+)',
            'lyricDesigner': r'リリックデザイン[：:]\s*([^\n\s]+)'
        }

        for key, pattern in patterns.items():
            match = re.search(pattern, desc)
            if match:
                creators[key] = match.group(1).strip()

        return creators

    def _extract_game_data(self, sekai_music: Dict) -> Dict:
        """提取游戏数据"""
        if not sekai_music:
            return {}

        return {
            'difficulty': {
                'easy': None,
                'normal': None,
                'hard': None,
                'expert': None,
                'master': None
            },
            'duration': None,
            'bpm': None,
            'publishedAt': sekai_music.get('publishedAt')
        }

    def build_database(self) -> Dict:
        """构建完整数据库"""
        print("\n🔨 开始构建数据库...")

        # 按歌曲名分组视频
        songs_dict = {}
        for video in self.youtube_data['videos']:
            song_title = self.extract_song_title(video['title'])

            if song_title not in songs_dict:
                songs_dict[song_title] = []
            songs_dict[song_title].append(video)

        print(f"📊 识别到 {len(songs_dict)} 首不同的歌曲")

        # 构建歌曲条目
        songs = []
        for song_title, videos in songs_dict.items():
            song_entry = self.build_song_entry(song_title, videos)
            songs.append(song_entry)

            # 更新统计
            self.stats['total_videos'] += len(videos)
            for video in videos:
                video_type = self.determine_video_type(video, song_title)
                self.stats['video_type_breakdown'][video_type] = \
                    self.stats['video_type_breakdown'].get(video_type, 0) + 1

            for unit in song_entry['classification']['units']:
                self.stats['unit_breakdown'][unit] = \
                    self.stats['unit_breakdown'].get(unit, 0) + 1

        self.stats['total_songs'] = len(songs)

        # 构建最终数据库
        database = {
            'metadata': {
                'version': '2.0.0',
                'generatedAt': datetime.now().isoformat(),
                'sources': [
                    'YouTube API (playlist_videos_20260122_154134.json)',
                    'Sekai Viewer musics.json',
                    'Sekai Viewer musicTag.json',
                    f'Manual aliases.json ({len(self.aliases)} songs)'
                ],
                'stats': {
                    'totalSongs': self.stats['total_songs'],
                    'totalVideos': self.stats['total_videos'],
                    'matchedSekai': self.stats['matched_sekai'],
                    'videoTypeBreakdown': self.stats['video_type_breakdown'],
                    'unitBreakdown': self.stats['unit_breakdown']
                }
            },
            'songs': songs
        }

        return database

    def save_database(self, database: Dict, output_path: str):
        """保存数据库"""
        output_file = Path(output_path)
        output_file.parent.mkdir(parents=True, exist_ok=True)

        with open(output_file, 'w', encoding='utf-8') as f:
            json.dump(database, f, ensure_ascii=False, indent=2)

        print(f"\n✅ 数据库已保存到: {output_file}")
        print(f"📊 文件大小: {output_file.stat().st_size / 1024:.2f} KB")

    def print_stats(self):
        """打印统计信息"""
        print("\n" + "="*60)
        print("📊 构建统计")
        print("="*60)
        print(f"总歌曲数: {self.stats['total_songs']}")
        print(f"总视频数: {self.stats['total_videos']}")
        print(f"匹配到 Sekai 数据: {self.stats['matched_sekai']}/{self.stats['total_songs']}")
        print(f"\n视频类型分布:")
        for vtype, count in self.stats['video_type_breakdown'].items():
            print(f"  {vtype}: {count}")
        print(f"\n组合分布:")
        for unit, count in sorted(self.stats['unit_breakdown'].items(), key=lambda x: -x[1]):
            print(f"  {unit}: {count}")
        print("="*60)


def main():
    # 设置基础路径
    base_path = r"C:\Users\10693\Desktop\并非工作内容\Project Sekai 2DMV Database"

    # 创建构建器
    builder = DatabaseBuilder(base_path)

    # 加载数据
    builder.load_data()

    # 构建数据库
    database = builder.build_database()

    # 保存数据库
    output_path = os.path.join(base_path, "output", "database_v2.json")
    builder.save_database(database, output_path)

    # 打印统计
    builder.print_stats()

    print("\n✨ 构建完成！")


if __name__ == '__main__':
    main()
```

---

### 阶段三：数据验证（第5天）

#### 3.1 创建验证脚本

**文件路径**: `scripts/validate_data.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
数据验证脚本
检查生成的数据库是否符合规范
"""

import json
from pathlib import Path
from typing import Dict, List

class DataValidator:
    def __init__(self, database_path: str):
        with open(database_path, 'r', encoding='utf-8') as f:
            self.database = json.load(f)

        self.errors = []
        self.warnings = []

    def validate(self):
        """执行所有验证"""
        print("🔍 开始数据验证...\n")

        self.validate_metadata()
        self.validate_songs()
        self.validate_videos()
        self.validate_references()

        self.print_report()

    def validate_metadata(self):
        """验证元数据"""
        print("📋 验证元数据...")
        metadata = self.database.get('metadata', {})

        required_fields = ['version', 'generatedAt', 'sources', 'stats']
        for field in required_fields:
            if field not in metadata:
                self.errors.append(f"metadata 缺少必填字段: {field}")

        if metadata.get('stats', {}).get('totalSongs', 0) != len(self.database.get('songs', [])):
            self.errors.append("metadata.stats.totalSongs 与实际歌曲数不符")

    def validate_songs(self):
        """验证歌曲数据"""
        print("🎵 验证歌曲数据...")

        song_ids = set()
        for idx, song in enumerate(self.database.get('songs', [])):
            # 检查必填字段
            required = ['id', 'title', 'videos']
            for field in required:
                if field not in song:
                    self.errors.append(f"歌曲 #{idx} 缺少必填字段: {field}")

            # 检查 ID 唯一性
            song_id = song.get('id')
            if song_id in song_ids:
                self.errors.append(f"重复的歌曲 ID: {song_id}")
            song_ids.add(song_id)

            # 检查视频列表
            if not song.get('videos'):
                self.warnings.append(f"歌曲 {song.get('title')} 没有关联的视频")

    def validate_videos(self):
        """验证视频数据"""
        print("📹 验证视频数据...")

        video_ids = set()
        for song in self.database.get('songs', []):
            for video in song.get('videos', []):
                # 检查必填字段
                required = ['type', 'videoId', 'url', 'title']
                for field in required:
                    if field not in video:
                        self.errors.append(
                            f"视频缺少必填字段: {field} (歌曲: {song.get('title')})"
                        )

                # 检查视频 ID 唯一性
                video_id = video.get('videoId')
                if video_id in video_ids:
                    self.warnings.append(f"重复的视频 ID: {video_id}")
                video_ids.add(video_id)

    def validate_references(self):
        """验证引用完整性"""
        print("🔗 验证引用完整性...")

        # 这里可以添加更多验证，例如：
        # - sekaiMusicId 是否存在于 Sekai 数据中
        # - units 名称是否合法
        # - virtualSingers 名称是否合法
        pass

    def print_report(self):
        """打印验证报告"""
        print("\n" + "="*60)
        print("📊 验证报告")
        print("="*60)

        if not self.errors and not self.warnings:
            print("✅ 验证通过！数据库结构完整。")
        else:
            if self.errors:
                print(f"\n❌ 发现 {len(self.errors)} 个错误:")
                for error in self.errors:
                    print(f"  - {error}")

            if self.warnings:
                print(f"\n⚠️  发现 {len(self.warnings)} 个警告:")
                for warning in self.warnings:
                    print(f"  - {warning}")

        print("="*60)


def main():
    database_path = r"C:\Users\10693\Desktop\并非工作内容\Project Sekai 2DMV Database\output\database_v2.json"

    validator = DataValidator(database_path)
    validator.validate()


if __name__ == '__main__':
    main()
```

---

### 阶段四：前端代码重构（第6-7天）

#### 4.1 备份旧版本

```bash
cp index.html backup/index_v1.html
```

#### 4.2 修改 index.html

在 `<script>` 标签中修改数据加载部分：

```javascript
// ===== 修改配置路径 =====
const JSON_DATA_PATH = './output/database_v2.json';

// ===== 删除或注释掉 Sekai Viewer 数据加载 =====
// 因为数据已经整合在 database_v2.json 中
// const SEKAI_MUSICS_PATH = './sekai viewer_json/musics.json';
// const SEKAI_MUSIC_TAGS_PATH = './sekai viewer_json/musicTag.json';
// const SEKAI_UNIT_PROFILES_PATH = './sekai viewer_json/unitProfiles.json';

// ===== 修改全局数据变量 =====
let data = null;  // 现在包含完整的 songs 数据

// ===== 修改数据加载函数 =====
async function loadDataFromJSON() {
    showLoading();

    try {
        const response = await fetch(JSON_DATA_PATH);

        if (!response.ok) {
            throw new Error(`HTTP错误! 状态码: ${response.status}`);
        }

        data = await response.json();

        // 验证数据结构（新结构）
        if (!data || !data.metadata || !data.songs) {
            throw new Error('JSON 数据格式不正确');
        }

        // 显示元数据
        document.getElementById('fetchTime').textContent =
            new Date(data.metadata.generatedAt).toLocaleString('zh-CN');
        document.getElementById('totalVideos').textContent =
            data.metadata.stats.totalVideos;
        document.getElementById('fetchMethod').textContent =
            'Database v2.0';
        document.getElementById('totalCount').textContent =
            data.metadata.stats.totalVideos;

        // 将 songs 转换为 videos 列表（用于兼容现有渲染逻辑）
        currentVideos = flattenSongsToVideos(data.songs);

        // 构建标签索引（新方法）
        buildTagIndexFromSongs();

        // 渲染视频卡片
        renderVideos(currentVideos);

        // 绑定筛选器事件
        setupFilters();
        setupFieldFilters();
        setupAdvancedFilters();

        console.log('✅ 数据加载成功！共加载', data.metadata.stats.totalSongs, '首歌曲，', data.metadata.stats.totalVideos, '个视频');

    } catch (error) {
        console.error('❌ 加载数据失败:', error);
        showError('无法加载数据库文件');
    }
}

// ===== 新增：将 songs 扁平化为 videos =====
function flattenSongsToVideos(songs) {
    const videos = [];

    songs.forEach(song => {
        song.videos.forEach(video => {
            // 为视频添加歌曲信息（方便搜索和显示）
            const enrichedVideo = {
                ...video,
                // 添加歌曲级别的信息
                songTitle: song.title,
                songAliases: song.aliases,
                songUnits: song.classification.units,
                songVirtualSingers: song.classification.virtualSingers,
                songTags: song.classification.tags,
                songCreators: song.creators,
                // 保留原有字段用于兼容
                position: video.playlistPosition,
                publishedAt: video.uploadDate
            };
            videos.push(enrichedVideo);
        });
    });

    return videos;
}

// ===== 修改标签索引构建 =====
function buildTagIndexFromSongs() {
    videoTags.clear();
    availableTags.clear();

    data.songs.forEach(song => {
        const tags = new Set(song.classification.tags || []);

        song.videos.forEach(video => {
            // 使用 playlistPosition 作为 key
            videoTags.set(video.playlistPosition, tags);

            // 统计标签数量
            tags.forEach(tag => {
                availableTags.set(tag, (availableTags.get(tag) || 0) + 1);
            });
        });
    });

    // 渲染标签筛选UI
    renderTagFilters();

    console.log('📊 标签索引构建完成:', availableTags.size, '个不同标签');
}

// ===== 修改搜索匹配函数（支持别称）=====
function matchNormal(video, term) {
    const titleMatch = video.title.toLowerCase().includes(term);
    const descMatch = video.description.toLowerCase().includes(term);
    const channelMatch = video.channelTitle.toLowerCase().includes(term);

    // 新增：别称匹配
    let aliasMatch = false;
    if (video.songAliases && video.songAliases.length > 0) {
        aliasMatch = video.songAliases.some(alias =>
            alias.toLowerCase().includes(term)
        );
    }

    // 新增：歌曲标题匹配
    const songTitleMatch = video.songTitle ?
        video.songTitle.toLowerCase().includes(term) : false;

    return titleMatch || descMatch || channelMatch || aliasMatch || songTitleMatch;
}

// ===== 删除或注释掉 Sekai 数据匹配相关函数 =====
// function loadSekaiData() { ... }  // 不再需要
// function matchVideoToSekaiMusic(video) { ... }  // 不再需要
// function extractTagsFromVideo(video) { ... }  // 改用预构建的标签
```

#### 4.3 更新字段筛选器（添加别称显示）

在字段筛选区域添加别称选项：

```html
<!-- 在 index.html 的字段筛选区域添加 -->
<div class="checkbox-item" data-field="aliases">
    <input type="checkbox" id="field-aliases">
    <label for="field-aliases">🏷️ 别称</label>
</div>
```

在 JavaScript 中添加别称字段处理：

```javascript
let displayFields = {
    thumbnail: true,
    title: true,
    aliases: false,  // 默认不显示
    channel: true,
    date: true,
    description: true
};

// 在 createVideoRow 函数中添加别称列
function createVideoRow(video) {
    const row = document.createElement('tr');

    // ... 现有代码 ...

    // 别称列
    if (displayFields.aliases) {
        const aliasesText = video.songAliases && video.songAliases.length > 0
            ? video.songAliases.join(', ')
            : '-';
        rowHTML += `<td class="video-aliases-cell">${aliasesText}</td>`;
    } else {
        rowHTML += '<td style="display:none;"></td>';
    }

    // ... 其他列 ...
}

// 在 updateTableHeaders 函数中添加
function updateTableHeaders() {
    const headers = {
        '.th-thumbnail': displayFields.thumbnail,
        '.th-title': displayFields.title,
        '.th-aliases': displayFields.aliases,  // 新增
        '.th-channel': displayFields.channel,
        '.th-date': displayFields.date,
        '.th-description': displayFields.description
    };

    // ... 现有代码 ...
}
```

在表格头部添加别称列：

```html
<thead>
    <tr>
        <th class="th-thumbnail">缩略图</th>
        <th class="th-title">标题</th>
        <th class="th-aliases">别称</th> <!-- 新增 -->
        <th class="th-channel">频道</th>
        <th class="th-date">发布日期</th>
        <th class="th-description">描述</th>
        <th class="th-action">操作</th>
    </tr>
</thead>
```

---

### 阶段五：测试与优化（第8-9天）

#### 5.1 功能测试清单

```markdown
## 测试清单

### 数据加载测试
- [ ] 数据库文件能否正确加载
- [ ] 元数据信息显示正确
- [ ] 统计信息准确

### 搜索功能测试
- [ ] 按标题搜索正常
- [ ] 按别称搜索正常
- [ ] 按描述搜索正常
- [ ] 正则表达式搜索正常

### 筛选功能测试
- [ ] 日期范围筛选正常
- [ ] 标签筛选正常（单个标签）
- [ ] 标签筛选正常（多个标签）
- [ ] 组合筛选（搜索+日期+标签）

### 排序功能测试
- [ ] 位置排序（升序/降序）
- [ ] 日期排序（最新/最旧）
- [ ] 标题排序（A-Z/Z-A）

### 字段显示测试
- [ ] 缩略图显示/隐藏
- [ ] 标题显示/隐藏
- [ ] 别称显示/隐藏
- [ ] 频道显示/隐藏
- [ ] 日期显示/隐藏
- [ ] 描述显示/隐藏

### 性能测试
- [ ] 页面加载速度 < 2秒
- [ ] 搜索响应速度 < 100ms
- [ ] 筛选响应速度 < 200ms
```

#### 5.2 性能优化建议

```javascript
// 1. 使用防抖优化搜索
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

// 应用到搜索输入
searchInput.addEventListener('input', debounce((e) => {
    applyFilters();
}, 300));

// 2. 虚拟滚动（如果视频数量超过1000）
// 考虑使用 react-window 或 vue-virtual-scroller

// 3. 懒加载缩略图
// 已经使用了 loading="lazy"，确保保留
```

---

### 阶段六：部署与上线（第10天）

#### 6.1 部署前检查

```bash
# 1. 确认所有文件存在
ls output/database_v2.json
ls manual_data/aliases.json
ls scripts/build_database.py

# 2. 运行构建脚本
cd scripts
python build_database.py

# 3. 运行验证脚本
python validate_data.py

# 4. 备份旧版本
mkdir -p ../backup
cp ../index.html ../backup/index_v1_$(date +%Y%m%d).html

# 5. 启动本地服务器测试
cd ..
python -m http.server 8000
# 访问 http://localhost:8000
```

#### 6.2 Git 版本控制

```bash
# 添加新文件
git add output/database_v2.json
git add manual_data/
git add scripts/
git add 数据库重构实施方案.md

# 提交
git commit -m "feat: 数据库重构 v2.0 - 整合歌曲数据与别称系统"

# 创建版本标签
git tag v2.0.0
```

---

## 📅 时间规划

| 阶段 | 任务 | 预计时间 | 负责人 |
|------|------|---------|--------|
| 1 | 数据准备：创建目录、aliases.json | 1-2天 | 开发者 |
| 2 | 脚本开发：build_database.py | 2天 | 开发者 |
| 3 | 数据验证：validate_data.py | 0.5天 | 开发者 |
| 4 | 前端重构：修改 index.html | 1.5天 | 开发者 |
| 5 | 测试优化：功能测试、性能优化 | 2天 | 开发者 + 测试 |
| 6 | 部署上线：备份、部署、文档 | 1天 | 开发者 |
| **总计** | | **8-10天** | |

---

## ⚠️ 风险与应对

### 风险一：数据匹配准确性
**风险描述**: 歌曲标题提取不准确，导致同一首歌被拆分成多个条目

**应对措施**:
1. 在 `corrections.json` 中维护标题映射
2. 人工审核构建结果
3. 提供合并工具脚本

### 风险二：性能问题
**风险描述**: 视频数量增加后，页面加载/筛选变慢

**应对措施**:
1. 实现虚拟滚动
2. 使用 Web Workers 处理筛选逻辑
3. 考虑分页加载

### 风险三：向后兼容
**风险描述**: 旧版本用户无法访问

**应对措施**:
1. 保留旧版本 index_v1.html
2. 提供版本切换链接
3. 逐步迁移用户

### 风险四：手动数据维护负担
**风险描述**: 每次新增歌曲都要手动添加别称

**应对措施**:
1. 开发别称管理界面（未来功能）
2. 从社区众包别称数据
3. 使用AI辅助生成别称建议

---

## 📈 后续扩展计划

### Phase 2: 多视频类型支持（1-2个月后）
- 添加 3DMV 视频
- 添加翻唱视频
- 添加演唱会 Live 视频
- 视频类型筛选器

### Phase 3: 高级功能（3-6个月后）
- 歌曲对比功能（同一首歌的不同版本）
- 歌曲收藏系统
- 播放列表生成
- 数据可视化（统计图表）

### Phase 4: 社区功能（6-12个月后）
- 用户评论系统
- 别称投票系统
- 数据贡献系统
- API 接口开放

---

## 📚 附录

### A. 完整文件结构

```
Project Sekai 2DMV Database/
├── index.html                          # 主页面（已重构）
├── 数据库重构实施方案.md               # 本文档
├── manual_data/                        # 手动维护数据
│   ├── aliases.json                   # 歌曲别称
│   ├── creators.json                  # 创作者补充信息
│   └── corrections.json               # 数据修正
├── scripts/                            # 构建脚本
│   ├── build_database.py              # 主构建脚本
│   ├── validate_data.py               # 验证脚本
│   └── utils.py                       # 工具函数
├── output/                             # 输出目录
│   └── database_v2.json               # 生成的数据库
├── backup/                             # 备份目录
│   └── index_v1_20260122.html         # 旧版本备份
├── fetch_youtube_playlist/             # YouTube 数据
│   └── playlist_videos_*.json
├── sekai viewer_json/                  # Sekai Viewer 数据
│   ├── musics.json
│   ├── musicTag.json
│   └── unitProfiles.json
└── (其他现有文件...)
```

### B. 关键配置参数

```python
# build_database.py 中的配置
CONFIG = {
    'base_path': r'C:\Users\10693\Desktop\并非工作内容\Project Sekai 2DMV Database',
    'youtube_data_file': 'playlist_videos_20260122_154134.json',
    'output_file': 'database_v2.json',
    'sekai_music_path': 'sekai viewer_json/musics.json',
    'sekai_tag_path': 'sekai viewer_json/musicTag.json',
    'aliases_path': 'manual_data/aliases.json',
    'corrections_path': 'manual_data/corrections.json'
}
```

### C. 常见问题 FAQ

**Q: 如果匹配不到 Sekai 数据怎么办？**
A: 系统会继续工作，只是该歌曲的 `sekaiMusicId` 为 null，标签会基于视频标题提取。

**Q: 如何添加新的别称？**
A: 编辑 `manual_data/aliases.json`，然后重新运行 `build_database.py`。

**Q: 可以回退到旧版本吗？**
A: 可以，旧版本已备份在 `backup/index_v1.html`。

**Q: 构建脚本运行失败怎么办？**
A: 检查文件路径是否正确，Python 版本是否 >= 3.7，查看错误日志。

---

## ✅ 验收标准

项目完成需满足以下所有标准：

- [ ] 数据库文件 `database_v2.json` 生成成功
- [ ] 所有歌曲都有至少一个视频
- [ ] 别称搜索功能正常工作
- [ ] 标签筛选功能正常工作
- [ ] 页面加载时间 < 2秒
- [ ] 搜索响应时间 < 100ms
- [ ] 通过所有功能测试
- [ ] 无 JavaScript 控制台错误
- [ ] 旧版本已备份
- [ ] 文档完整（本方案 + 代码注释）

---

## 📞 联系与支持

如有问题，请联系：
- 项目负责人：[你的名字]
- Email: [你的邮箱]
- GitHub Issue: [项目地址]

---

**文档版本**: v1.0
**最后更新**: 2026-01-22
**作者**: Claude (Anthropic)
